extends layout

block content
    h1= article.title
    h5 Written by #{article.author}
    p= article.body
    hr
    a.button.btn.btn-warning(type='button', href='/articles/edit/'+article._id) Edit
    a.button.btn.btn-danger.delete-article(type='button', data-id=article._id) Eliminar